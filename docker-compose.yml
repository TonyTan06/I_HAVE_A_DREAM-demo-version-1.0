services:
  game:
    build:
      context: .
    ports:
      - "6080:6080"
    environment:
      SCREEN_WIDTH: "1280"
      SCREEN_HEIGHT: "720"
      LIBGL_ALWAYS_SOFTWARE: "1"
      MESA_LOADER_DRIVER_OVERRIDE: "llvmpipe"
